# springboot-grpc-example

## How to build?

On root folder:

```
mvn clean install
```

## How to run?

Run server:


```
cd grpc-server
mvn spring-boot:run
```

Run client:

```
cd grpc-client
mvn spring-boot:run
```